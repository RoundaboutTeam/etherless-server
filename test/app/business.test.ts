/* example
import app from '../../app';

const assert = require('assert');

describe('Test test', () => {
  it('should return hello test', () => {
    const s = app.testFunction();
    assert.equal(s, 'hello test');
  });
});
*/
